services:
  app:
    build: . # 同じフォルダにあるDockerfileを元に「自分で組み立てる」という意味
    ports:
      - "8002:80"
    volumes:
      - ./:/var/www/html

  db:
      image: mysql:8.0
      environment:
        MYSQL_DATABASE: meal_planner
        MYSQL_ROOT_PASSWORD: root
      ports:
        - "3309:3306"